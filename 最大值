int max(int ,int ,int)

#include <stdio.h>
int main()
{
  int a, b, c;
  int max;
  scanf("%d%d%d", &a, &b, &c);
  max = max(a, b, c);
  printf("%d", max);
  return 0;
}

int max(int a, int b, int c)
{
    int max;
    max = (a > b) ? a : b;
    max = max > c ? max : c;
    return max;
}
